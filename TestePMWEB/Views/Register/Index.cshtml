@{
    ViewData["Title"] = "Registrar";
}
<h1>Registar</h1>
<hr />





<div class="container">
    <div class="row">
        <section>
            @using (Html.BeginForm("RegisterUser", "Autoriza", FormMethod.Post))
            {
                <div class="form-group">
                    <div class="col-md-10">
                        <p>Email:</p>
                        <input type="text" name="email" />
                        <p>Senha:</p>
                        <input type="password" name="password" />
                        <p>Confirmar Senha:</p>
                        <input type="password" name="confirmpassword" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-10">
                        <input type="submit" value="Efetuar Registro" />
                    </div>
                </div>
            }
        </section>
    </div>
</div>
